# Copy this file to `.env.local` (which is git-ignored).
# In dev you can leave REACT_APP_API_URL empty — the CRA proxy in package.json
# forwards /api requests to http://localhost:4000 automatically.
REACT_APP_API_URL=
PORT=3003
